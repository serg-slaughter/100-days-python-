# If the bill was $150.00, split between 5 people, with 12% tip.

# Each person should pay (150.00 / 5) * 1.12 = 33.6
# Format the result to 2 decimal places = 33.60

# Tip: There are 2 ways to round a number. You might have to do some Googling to solve this.💪

# Write your code below this line 👇

bill = float(input('what was the total bill?'))

# print(type(bill))

tip = int(input('waht percentage you like to give? 10, 12 or 15?'))

total = bill + (bill * tip / 100)
print(total)

people = int(input('number of person '))

split = total / people
print(split)

split1 = round(split, 2)
print(f"each person paying: {split1}")# If the bill was $150.00, split between 5 people, with 12% tip.

# Each person should pay (150.00 / 5) * 1.12 = 33.6
# Format the result to 2 decimal places = 33.60

# Tip: There are 2 ways to round a number. You might have to do some Googling to solve this.💪

# Write your code below this line 👇

bill = float(input('what was the total bill?'))

# print(type(bill))

tip = int(input('waht percentage you like to give? 10, 12 or 15?'))

total = bill + (bill * tip / 100)
print(total)

people = int(input('number of person '))

split = total / people
print(split)

split1 = round(split, 2)
print(f"each person paying: {split1}")
